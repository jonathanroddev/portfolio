import { FC } from 'react';
import Navbar from '../components/Navbar';

const Header: FC = () => {
    return (
        <header>
            <Navbar />
        </header>
    )
}

export default Header;